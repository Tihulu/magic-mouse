# Example user-service override only. It helps only if the daemon reads MAGIC_MOUSE_WS_CMD.
# Easier install:
#   magic-mouse-gnome-integrate systemd-env

[Service]
Environment=MAGIC_MOUSE_WS_CMD=%h/.local/bin/magic-mouse-ws
